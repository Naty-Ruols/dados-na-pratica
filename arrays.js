var valores = [3,5,2,5,1];

console.log(valores[0]);

for (var pos = 0 ; pos < valores.length; pos++){
    console.log ("Posição:" + pos + "Valor:" +[pos])
}

console.log("array invertido:" + valores.reverse());

var arr1 = [1,2,3,4,5]
console.log(arr1.join(' é menor que '));